const CardWidget = ({cantCarrito}) => {
    return (
        <>
            
            <botton className= "btn btn-dark">Carrito</botton>
            
            <p>{cantCarrito}</p>
        </>
    );
}

export default CardWidget;
